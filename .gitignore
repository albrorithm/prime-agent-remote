node_modules/
dist/
dist-server/
coverage/
.env
.env.*
!.env.example
.DS_Store
.research/
.analysis-repos/
.audit-source/
.ui-harness/
*.log
/start-live.local.sh
.playwright-mcp/
.dist-previous/
.dist-server-previous/
# Machine-local Claude Code permissions. Ignored globally on one machine, which
# is not protection a public repo can rely on: the file holds literal tokens
# from local runs, and a contributor without that global rule would commit it.
.claude/settings.local.json
